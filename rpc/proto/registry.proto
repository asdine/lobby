syntax = "proto3";

package proto;

import "types.proto";

service RegistryService {
  rpc Create (NewBucket) returns (Empty) {}
}

message NewBucket {
  // @inject_tag: valid:"required"
  string name = 1;
  // @inject_tag: valid:"required"
  string backend = 2;
}
